services:
  documents-service-db:
    image: mongo:6-jammy
    ports:
      - 27017:27017
    volumes:
      - documents-service-db:/var/lib/mongodb/data
    container_name: documents_service_db

volumes:
  documents-service-db: